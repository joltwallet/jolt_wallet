pipeline {
    agent any
    stages {
        stage('Build Docker Image') {
            steps {
                bash '''#!/bin/bash
                 echo "meow"
                '''
            }
        }
    }
}

