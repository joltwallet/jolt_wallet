
# TODO

* Change bluetooth driver to nimBLE; potentially saving 280KB off of firmware.

# Notes

packets_per_second = 0.1s * packets_per_connection_interval / connection_interval

packets_per_connection_interval = max 4 per direction for iOS


# ESP Actions
* Set min and max interval in adv data.

